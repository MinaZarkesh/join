class Divinput{
    spanClass = `d-none`;
    div_input;

    constructor(parent, name, placeholder, id, cssClass, type) {
        new Div(parent, name)
        new Input(name, id, cssClass, type, placeholder);
    }
}


